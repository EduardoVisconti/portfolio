# Gemini API key for the Ask Me Anything console (/api/chat).
# Free tier, no credit card: create one at aistudio.google.com/apikey
# Without this the console returns 503 and the client shows its fallback copy.
GEMINI_API_KEY=
