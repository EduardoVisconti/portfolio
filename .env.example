# Anthropic API key for the Ask Me Anything console (/api/chat).
# Billed separately from a Claude Pro subscription - add credits at
# console.anthropic.com. Without this the console renders its fallback copy.
ANTHROPIC_API_KEY=sk-ant-your-key-here
